# Machine Failure Prediction App

This project builds a machine learning web application to predict machine failure using operating parameters.

## Tools
Python
Pandas
Scikit-learn
Streamlit

## Features
User enters machine parameters and the model predicts failure risk.

## Run the App

pip install -r requirements.txt

streamlit run app.py (or) py -m streamlit run app.py
